The example is located in the main record package.
The example app for this package is maintained in the upstream `record` package.

This directory is kept only to preserve the package layout used by the local Flutter dependency override.
